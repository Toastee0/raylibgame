#ifndef CELL_DEFAULTS_H
#define CELL_DEFAULTS_H

#include "cell_types.h"

// Initialize a cell with default values based on its type
void InitializeCellDefaults(GridCell* cell, int type);

#endif // CELL_DEFAULTS_H